{
  "buildCommand": "ls -la && pwd && cd frontend && npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/dist/* public/ && echo 'Build complete' && ls -la public",
  "outputDirectory": "public"
}
