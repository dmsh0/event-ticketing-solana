{
  "buildCommand": "npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/dist/* public/ && echo 'Build complete - checking public dir' && ls -la public",
  "outputDirectory": "public"
}
